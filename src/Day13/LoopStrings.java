package Day13;

public class LoopStrings {
    public static void main(String[] args) {

        String word = "hello";
        String reversed = "";


        for (int i = word.length() - 1; i >= 0; i--){
            reversed += word.charAt(i);
        }


















//        reversedWord = reversedWord + word.charAt(7); //e
//        reversedWord = reversedWord + word.charAt(6); //es
//        reversedWord = reversedWord + word.charAt(5); //esi
//        reversedWord = reversedWord + word.charAt(4); //esiw
//        reversedWord = reversedWord + word.charAt(3); //esiwe
//        reversedWord = reversedWord + word.charAt(2); //esiwed
//        reversedWord = reversedWord + word.charAt(1); //esiwedo
//        reversedWord = reversedWord + word.charAt(0); //esiwedoC















    }
}
