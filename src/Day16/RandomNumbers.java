package Day16;

import java.util.Random;

public class RandomNumbers {
    public static void main(String[] args) {
        int counter = 0;
        String word = "hello12";
        // int counter = 0;
        // int i = 0;
        // boolean wordIsFinished; false -> still checking a word
        // boolean wordIsFinished; true -> we are ready to enter a new word
        // do {
        // if(wordIsFinished){
        //  enter a new word;
        // i = 0;
        // }
        // ask to enter a word -> hello12
        // char symbol = word.charAt(i) -> 'e'
        // if(Character.isDigit(symbol)) then counter++
        //
        //
        //  i++;
        // } while(counter!=3);

    }
}
